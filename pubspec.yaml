name: sa_fgbg_listener_lego
description: "lego that fg/bg listener"
#publish_to: 'none'
repository: https://github.com/flutter-lego/sa_fgbg_listener_lego

version: 1.0.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: #@add
  gap: #@add
  june_flow_util:
  styled_widget: #@add
  modal_bottom_sheet: #@add
  keyboard_attachable: #@add
  flutter_zoom_drawer: #@add
  flutter_screenutil:
  bot_toast:
  flutter_fgbg:
  easy_event_bus:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  screenutil_lego: #@add
  bot_toast_lego: #@add
  flutter_fgbg_lego: #@add
  sa_listener_lego:
  listener_lego:

flutter:
  assets:
    - assets/lego/sa_fgbg_listener_lego/
  uses-material-design: true

topics:
  - lego